spring:
  # 禁用 banner
  main:
    banner-mode: off
  threads:
    virtual:
      enabled: true

logging:
  config: classpath:logback/local.xml

pangju:
  ocr:
    engine: tesseract_cli
    tesseract_cli:
      #      path: D:\dev-tool\Tesseract-OCR\tesseract.exe
      #      data-path: E:\tessdata_data
      language: chi_sim
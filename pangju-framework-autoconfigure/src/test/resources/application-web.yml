server:
  port: 80
  servlet:
    context-path: /
    encoding:
      force: true

spring:
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 110MB

logging:
  config: classpath:local.yml